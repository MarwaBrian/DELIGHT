from django.db import models

# Create your models here.
#each cart should have a one to one relationship with the user(foreign key)
#carts should have a many to many relationship with different objects, in this case different cakes and cookies in the products section(delight_Cakehouse)

